# {{ project_name }}

Generated by **perfectRAG** — template `custom-naive-rag`.

## Stack

| Component | Choice |
|---|---|
| LLM | `{{ recipe.llm_model }}` (via {{ recipe.llm_runtime }}) |
| Embedding | `{{ recipe.embedding_model }}` |
| Vector DB | Qdrant |
| Doc parser | `{{ recipe.doc_parser }}` |
| Chunking | {{ recipe.chunk_strategy }} / {{ recipe.chunk_size }} tokens |
| Chat UI | open-webui (http://localhost:3000) |
| API | FastAPI (http://localhost:8000) |

## Quickstart

```bash
docker compose up -d
# lần đầu: đợi service `ollama-pull` kéo model về (vài GB)
docker compose logs -f ollama-pull
```

Mở `http://localhost:3000` để chat (open-webui), `http://localhost:8000/docs` cho API.

## Ingest documents

```bash
curl -F "file=@./data/sample.pdf" http://localhost:8000/ingest
```

## Query

```bash
curl -X POST http://localhost:8000/query \
  -H "Content-Type: application/json" \
  -d '{"question": "What is RAG?"}'
```

## Evaluate retrieval quality

```bash
# edit eval/datasets/retrieval-golden.jsonl with real questions + relevant sources
perfectrag eval --retrieval -d eval/datasets/retrieval-golden.jsonl --gate
```

Reports recall@k / MRR / nDCG; `--gate` exits non-zero below thresholds (CI gate).

## Add MCP tool

```bash
perfectrag add mcp filesystem --project .
```

Xem `mcp.yaml` để cấu hình. Các MCP server có sẵn: `perfectrag list mcp`.

## Add skill

```bash
perfectrag add skill legal-rag --project .
```

Skills sẽ được mount read-only vào container `app`.
