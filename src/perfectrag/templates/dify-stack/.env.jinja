PROJECT_NAME={{ project_name }}

# Secrets — generate your own for production
SECRET_KEY=sk-dify-{{ project_name }}-replace-me
DB_PASSWORD=dify_pg_pw
REDIS_PASSWORD=dify_redis_pw

# LLM wiring (Ollama)
OLLAMA_URL=http://ollama:11434
DEFAULT_LLM={{ recipe.llm_model }}
DEFAULT_EMBEDDING=nomic-embed-text

# Hardware hints
GPU_ENABLED={{ 'true' if recipe.gpu_enabled else 'false' }}
VRAM_CAP_GB={{ recipe.vram_cap_gb }}
