# {{ project_name }} — LightRAG / GraphRAG stack

Generated by **perfectRAG** — template `lightrag-stack`.

**Backbone:** [LightRAG](https://github.com/HKUDS/LightRAG) — GraphRAG với dual-level retrieval, cost-efficient (~1/100 Microsoft GraphRAG).

## Stack

| Component | Choice |
|---|---|
| GraphRAG engine | LightRAG latest |
| LLM | `{{ recipe.llm_model }}` (via Ollama) |
| Embedding | nomic-embed-text (Ollama) |
| Graph storage | NetworkX (in-process; switch Neo4j for scale) |
| Vector storage | NanoVectorDB (bundled) |
| UI | LightRAG WebUI (port 9621) + open-webui chat (port 3000) |

## Quickstart

```bash
docker compose up -d
docker compose logs -f ollama-pull
```

- LightRAG WebUI (ingest + graph visualize): `http://localhost:9621`
- open-webui chat: `http://localhost:3000`

## Ingest documents

Thả file vào `./inputs/` — LightRAG WebUI sẽ detect và index.

Hoặc qua API:

```bash
curl -X POST http://localhost:9621/documents/file \
  -F "file=@./inputs/sample.pdf"
```

## Query (dual-level)

LightRAG hỗ trợ 4 modes: `naive`, `local`, `global`, `hybrid`.

```bash
curl -X POST http://localhost:9621/query \
  -H "Content-Type: application/json" \
  -d '{"query": "Summarize the main entities", "mode": "hybrid"}'
```

## Hardware hint

{% if hw.vram_gb < 12 and hw.gpu_vendor == 'nvidia' -%}
GPU của bạn có {{ hw.vram_gb }} GB VRAM — GraphRAG entity extraction sẽ chậm.
Cân nhắc upgrade lên ≥12 GB hoặc giảm corpus.
{%- elif hw.gpu_vendor == 'none' -%}
CPU-only — GraphRAG sẽ rất chậm. Đề xuất dùng template `custom-naive-rag` thay thế.
{%- else -%}
Hardware phù hợp ({{ hw.gpu_vendor }} / {{ hw.vram_gb }} GB VRAM).
{%- endif %}
