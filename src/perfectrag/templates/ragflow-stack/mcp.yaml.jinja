# MCP servers available to this RAGFlow project.
# RAGFlow hỗ trợ MCP native — servers dưới đây sẽ được surface như tools trong RAGFlow UI.
servers:
  filesystem:
    command: npx
    args: ["-y", "@modelcontextprotocol/server-filesystem", "./data"]
