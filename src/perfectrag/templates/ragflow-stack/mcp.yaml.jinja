# MCP servers available to this RAGFlow project.
# RAGFlow supports MCP natively — the servers below are surfaced as tools in the RAGFlow UI.
servers:
  filesystem:
    command: npx
    args: ["-y", "@modelcontextprotocol/server-filesystem", "./data"]
